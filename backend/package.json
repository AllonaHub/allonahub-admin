{
  "name": "@allonahub/backend-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AllonaHub production backend API for Hetzner CPX31.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config.js && node --check src/lib/iyzico.js && node --check src/lib/supabase.js && node --check src/routes/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "@supabase/supabase-js": "^2.45.4",
    "fastify": "^4.28.1",
    "zod": "^3.23.8"
  }
}
